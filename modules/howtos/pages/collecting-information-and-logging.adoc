= Logging
:description: The Rust SDK logs events via the `tracing` crate.
:page-toclevels: 2

[abstract]
{description}
It has no hard dependency on a specific logger implementation, but setting up https://docs.rs/tracing-subscriber/latest/tracing_subscriber/[`tracing-subscriber`] is recommended.

== Configuring Logging

The Couchbase Rust SDK emits all log messages as https://docs.rs/tracing/latest/tracing/[`tracing`] events.
It also enables the https://docs.rs/tracing/latest/tracing/#emitting-log-records[`log` compatibility feature], which means any logger configured via the https://docs.rs/log/latest/log/[`log`] crate (such as `env_logger`) will also receive SDK log messages.
However, setting up a https://docs.rs/tracing-subscriber/latest/tracing_subscriber/[tracing-subscriber] directly is the recommended approach as it allows you to leverage the tracing spans and metrics the SDK emits.

=== Recommended setup with tracing-subscriber

Add the following to your `Cargo.toml`:

[source,toml]
----
[dependencies]
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
----

Then register a subscriber before connecting to Couchbase:

[source,rust]
----
include::devguide:example$src/logging.rs[tag=tracing-logging,indent=0]
----
NOTE: The above setup also enables the xref:observability-tracing.adoc[`ThresholdLoggingTracer`] and xref:observability-metrics.adoc[`LoggingMeter`], these are optional,
however it is highly recommended that these are enabled if you are not using any other tracers or meters to help aid with debugging.

With this setup you can control verbosity at runtime via the `RUST_LOG` environment variable:

[source,console]
----
$ RUST_LOG='couchbase=debug' cargo run
----

=== Alternative: 'log' compatible loggers

If you prefer to stay with the https://docs.rs/log/latest/log/[log] ecosystem, the SDK has the https://docs.rs/tracing/latest/tracing/#crate-feature-flags[log] feature of the tracing crate enabled, meaning that any logger which consumes `log` records, such as the `env_logger` will work without any extra configuration:

[source,toml]
----
[dependencies]
env_logger = "0.11"
----

[source,rust]
----
include::devguide:example$src/logging.rs[tag=env-logger,indent=0]
----

Set `RUST_LOG` to control verbosity:

[source,console]
----
$ RUST_LOG='couchbase=debug' cargo run
----

TIP: In addition to the special targets mentioned below, when logging at 'trace' level with the `log` compatible loggers, you may wish to also filter out the `tracing::span` target, which logs each time spans are entered and exited, as this can add a lot of noise to the logs.

[#special-targets]
=== Special log targets

The SDK emits events under two special targets that deserve attention:

`couchbase::tracing`::
The SDK orchestrates `tracing` https://docs.rs/tracing/latest/tracing/span/index.html[spans] to this target at trace level. Filter this target off on your logger if you don't want span noise in your log output.
Note that this target _is_ still needed if you have any layers consuming SDK spans, such as the xref:observability-tracing.adoc[ThresholdLoggingTracer or OpenTelemetry layer] in your subscriber stack -- `EnvFilter` filtering can be done per-layer, so other layers can still receive them.

`couchbase::metrics`::
Internal metrics events intended exclusively for metrics backends such as the xref:observability-metrics.adoc[`LoggingMeter` or OpenTelemetry `MetricsLayer`].
These are emitted at `trace` level only and produce no useful output in plain log format, so it is recommended to configure your logger to turn this off.



////
== SDK Telemetry from the Server

In addition to Tracing and other metrics, and client logging, SDK is telemetry is also sent to the Server -- available from 8.0, and in new Capella Operational clusters -- for ingestion with other Prometheus metrics.
Capella Operatioal exposes these metrics through the UI.

For self-managed Server, collection can be disabled and enabled through the REST API:

[source,console]
----
curl --user Administrator:password http://172.17.0.2:8091/settings/appTelemetry -d enabled=true
----

And the Prometheus-format metrics fetched with:

[source,console]
----
curl --user Administrator:password http://172.17.0.2:8091/metrics
----

There may be advantages to collecting information this way,
but note that metrics are collected per node,
and a central Prometheus instance should be set to collect all metrics so that information is not lost in case of a sudden failover.

Also note that if the cluster is behind a load balancer, the collected metrics may not accurately record the actual correct node with which the SDK interacts.
////
